这一段是 `P9` 的收口：workload 选型练习、验收标准、常见卡点，以及进入 `P10` 锁存机制前的索引知识拼图。**P9-4 开始**

**今天这次的目标**
- 确认你能按 workload 选型，并动手完成至少一个 P9 最小实验（Bloom 或跳表或倒排）。
- 把 P7/P8/P9 的索引地图拼完整，再进入 P10。

**P9 前三段串起来**
- `P9-1`：为何没有万能索引；四种结构定位。
- `P9-2`：倒排、Bloom、跳表、ANN 机制。
- `P9-3`：边界、最小实现、实验方法。

---

**推荐练习清单**

**对应 Code 目录**
- 这一段如果要动手实现，代码统一放在 `Code/p9_aux_indexes/`。
- 这一课在 `Code/` 里的主题是：`倒排、Bloom、跳表、向量索引的 toy 实验`。
- 建议起手顺序：先选其中一个结构做教学版最小实现，不必全做。

**练习 A：选型题（纸上）**
- 对 8 个场景各选结构（B+Tree / Hash / 倒排 / Bloom / 跳表 / ANN / SeqScan），写一句理由：
  - 主键点查
  - 时间范围报表
  - 商品标题关键词搜索
  - LSM 10 层 SSTable 点查
  - MemTable 实时有序插入
  - 图像 embedding top-20
  - 判断 rid 是否可能在某 page（辅助）
  - 全表 aggregate 无 where

**练习 B：mini 倒排**
- 3 篇英文短文，实现 Tokenize + Index + AND 查询。

**练习 C：Bloom 实验报告**
- 固定 m，变 n，画 fp rate 曲线；写 200 字结论。

**练习 D：跳表对拍**
- 10000 步随机 Insert/Remove/Search，对照 std::set。

**练习 E：短文**
- “为什么 bustub 教 B+Tree 但不教向量索引”——从课程目标回答。

---

**P9 整体验收标准**

**理论**
- 按 workload 说明何时选 B+Tree、hash、倒排、Bloom、跳表、ANN。
- 解释 Bloom 为何不会漏报但会误报。
- 区分精确索引与近似索引的正确性目标。
- 说明倒排 posting list 的作用与 AND 算法。

**实践（至少一项）**
- 完成 Bloom 测 fp、或跳表对拍、或 mini 倒排 AND。
- 能解释实验现象与 P9-2 理论是否一致。

---

**验收自测题**
- 1. 全文检索为何不用 B+Tree 扫 doc_id？
- 2. Bloom 说 false 和 true 时各应做什么？
- 3. 跳表与 B+Tree 在存储介质上的典型差异？
- 4. recall@K 定义？
- 5. 倒排索引更新一篇 doc 的 term 变化时难点是什么？
- 6. P7/P8/P9 在“精确 vs 近似”上如何分类？

---

**常见卡点**
- `只会背结构名，不会选型` -> 重做练习 A。
- `把 Bloom 当索引` -> 重读 P9-2 二次确认。
- `ANN 不谈 recall` -> 任何向量实验必须对比 brute。
- `忽视 P7/P8 就跳 P9` -> P9 是扩展，不是替代 B+Tree/hash 基础。

---

**索引知识拼图（P7–P9）**

```text
P7 Hash        等值精确索引（磁盘 extendible hash）
P8 B+Tree      有序精确索引（范围/排序）
P9 倒排        term 检索
P9 Bloom       存在性辅助过滤
P9 跳表        内存有序写路径
P9 ANN         相似度近似检索
```

- 下一课 P10 不再新索引，而是问：
- `这些结构在多线程下怎样用 latch 保护而不 corrupt？`

---

**20 分钟 / 90 分钟复习计划**
- 20 分钟：
- 重做练习 A 中 8 个场景的索引选型，不许查表。
- 用一句话解释 Bloom false 与 true 的语义。
- 说出倒排 AND 查询为什么需要 posting 有序。

- 90 分钟：
- 完成练习 C（Bloom 曲线）或练习 D（跳表对拍）之一，写 5 句实验结论。
- 读一篇 LSM + Bloom 或 IVF 科普，标注与 P9-2 的对应关系。
- 做验收自测 6 题，错题回到 P9-1 选型框架。

---

**实验报告模板（若交作业或自学记录）**
- 标题：结构名 + 实验目的（如“Bloom 假阳性随 n/m 变化”）。
- 设置：m, k, n 取值范围，随机种子。
- 结果：一张表或曲线，empirical fp vs 理论 p。
- 结论：是否无漏报；fp 何时高到不可用；与 LSM 读路径的关系。
- 反思：该结构**不能**替代 B+Tree 的场景是什么。

---

**与工业系统的映射（扩展阅读）**
- Elasticsearch：倒排 + 压缩 posting；可选 dense_vector 做 ANN。
- RocksDB：MemTable（跳表/skiplist 实现）+ SSTable + Bloom filter。
- PostgreSQL：B+Tree（GiST/GIN 等扩展索引另说）；pgvector 插件做 ANN。
- 这些名字不必背，但要能说出：`P9 不是孤立技巧，是真实系统的组件词汇。`

---

**今天你必须真正吃透的 3 句话**
- `P9 教选型与扩展视野，不取代 P7/P8 核心项目。`
- `验收要会用 workload 语言说话，并至少做一个 mini 实验。`
- `Bloom/ANN 的正确性语义和 B+Tree 不同，不能混谈。`

---

**P9 与 P7/P8 的对照表（收口用）**

```text
维度           Hash(P7)     B+Tree(P8)    倒排(P9)     Bloom(P9)
------------------------------------------------------------------
查询           等值         等值+范围      term 布尔     存在性
精确性         精确         精确           精确(term)    辅助/误报
磁盘主结构     是           是             常是          常嵌入元数据
bustub 主线    项目         项目           扩展          扩展
更新代价       split        split/merge    posting 维护  只增
```

- 这张表的目标不是背下来，而是训练你**第一眼看到 SQL 就想到结构**。

**面试 / 口试常问（自选准备）**
- “Bloom 能代替 hash index 吗？”——不能，有误报且不提供 rid。
- “向量检索为何不用 B+Tree？”——高维无全序，要 ANN 换 recall。
- “跳表和 B+Tree 区别？”——跳表典型在内存写路径；磁盘有序主索引是 B+Tree。

---

**若 P9 仍感觉抽象：一条最小学习路径**
- 第 1 天：P9-1 选型框架 + 练习 A 八场景。
- 第 2 天：P9-2 倒排 + Bloom 机制，实现 mini 倒排 AND。
- 第 3 天：Bloom 测 fp 曲线或跳表对拍，写 5 句结论。
- 第 4 天：P9-4 验收自测 + 对照 P7/P8 索引地图。
- 不必四项结构都写生产级，**至少深度做一个实验**即可过关。

**进入 P10 前的最后一问**
- 你已经能区分“精确索引”和“辅助/近似结构”了吗？
- 若仍把 Bloom 当主索引，或认为 B+Tree 能高效做向量 top-K，建议重读 P9-1 再进 P10。
- P10 的 latch 不解决选型问题，它只保护你已选结构的 page 在并发下不被写坏。

---

**小练习（收口版）**
- 练习 1：8 场景选型（同练习 A），闭卷重做。
- 练习 2：写 100 字说明“为何 ANN 实验必须报告 recall@K”。
- 练习 3：LSM 点查时 Bloom 放在 SSTable 元数据层，描述一次 false negative 与 false positive 各会怎样（各一句话）。

---

**自测问题（收口）**
- 你能用 workload 五步法（P9-1）分析一个新查询该用什么索引吗？
- 你能解释跳表为何常见于 MemTable 而不是磁盘主索引吗？
- 若产品要求“搜索关键词 + 按时间排序”，你会组合哪两种结构？

---

**下一步怎么学**
- `P10`：latch vs lock、共享/独占、latch crabbing、死锁——直接服务 bustub 并发 B+Tree。
